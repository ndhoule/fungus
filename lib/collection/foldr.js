import { curry } from '../function/curry';

/**
 * TODO
 *
 * @name foldr
 * @api public
 * @category Collection
 * @param {Function} fn
 * @param {Object|Array} collection
 * @param {*} acc
 * @return {undefined}
 */
export let foldr = curry(function foldr(fn, collection, acc) {});
