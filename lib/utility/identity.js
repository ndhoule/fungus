/**
 * A function that returns whatever is passed to it.
 *
 * @param {*} val The input value.
 * @return {*} The input value, `val`.
 */
export let identity = function identity(val) {
  return val;
};
