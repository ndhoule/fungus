/**
 * A function which does nothing (no operation).
 *
 * @name noop
 * @api public
 * @category Utility
 * @return {undefined}
 * @example
 * noop();
 * //=> undefined
 *
 * noop(1);
 * //=> undefined
 */
let noop = function noop() {};

export default noop;
