import compact from './compact';
import filter from './filter';
import first from './first';
import rest from './rest';

export default {
  compact: compact,
  filter: filter,
  first: first,
  rest: rest
};
