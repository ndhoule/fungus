import foldl from './foldl';
import forEach from './forEach';
import groupBy from './groupBy';
import map from './map';

export default {
  foldl: foldl,
  forEach: forEach,
  groupBy: groupBy,
  map: map
};
