module.exports = {
  colors: true,
  reporters: ['progress'],
  // TODO: Rebuild files on test runs, run this in continuous mode
  autoWatch: false,
  singleRun: true,
  browsers: ['Chrome', 'Firefox']
};
